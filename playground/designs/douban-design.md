# Design System: 豆瓣评分-首页

## 1. 视觉主题与氛围

豆瓣评分-首页 的网站呈现出浅色主题、简洁、扁平设计、克制的色彩运用的设计风格。页面以浅色画布（`#ffffff`）为底，主文本颜色为 `#000000`。主强调色为 `#efefef`，广泛应用于交互元素和关键视觉锚点。

主字体为 `system font`. 

> Design tokens extracted from local source code at /Users/hexford/claw_test/design_skill/3505013644

**关键特征：**
- 主字体: `system font`
- 页面背景: `#ffffff`
- 主文本颜色: `#000000`
- 主强调色: `#efefef`
- 扁平设计，极少使用阴影
- 最常用的圆角半径: `10rpx`
- 默认浅色主题


## 2. 色彩体系与角色

### 主色
- **页面背景** (`#ffffff`): 页面主背景色。
- **主文本** (`#000000`): 默认正文文本颜色。

### 强调色
- **强调色 #42bd56** (`#42bd56`): 用于background，出现 1 次。

### 中性色阶
- **中性色 94%** (`#efefef`): 用于background, border，出现 3 次。
- **中性色 60%** (`#999999`): 用于border，出现 1 次。
- **中性色 29%** (`#494949`): 用于，出现 1 次。
- **中性色 70%** (`#b2b2b2`): 用于background，出现 1 次。

### 表面与边框
- **边框色 #05b512** (`#05b512`): 边框颜色，出现 3 次。



## 3. 排版规则

### 字体族

### 层级体系

| 角色 | 字体 | 字号 | 字重 | 行高 | 字间距 | 备注 |
|------|------|------|--------|-------------|----------------|-------|

### 原则
- 主字体 `system` 应用于所有主要文本元素
- 使用的字重：


## 4. 组件样式

### 按钮

**主要**
- Background: `rgba(0,0,0,.7)`
- Text: `#999`
- Padding: `30rpx`
- Radius: `10rpx`
- Font: 32rpx null weight 700
- Border: `2rpx solid #efefef`

### 卡片与容器

未检测到明显的卡片/容器样式。

### 输入框与表单

首页未检测到表单输入框。

### 导航

未检测到导航元素。


## 5. 布局原则

### 间距系统
- 基础单位: `8px`

### 栅格与容器
- 最大内容宽度: `100%`
- CSS Grid 使用: 0 个元素
- Flexbox 使用: 3 个元素
- 主要布局方式: Flexbox

### 留白哲学
- **均衡间距**：设计在信息密度和呼吸空间之间取得了平衡。

### 圆角比例尺
| 圆角值 | 使用次数 |
|--------|-------------|
| `10rpx` | 1 |


## 6. 层深与阴影

该设计采用扁平风格，未检测到明显的 box-shadow 使用。

| 层级 | 处理方式 | 用途 |
|-------|-----------|-----|
| 扁平（层级 0） | 无阴影 | 所有元素 |


## 7. 设计规范与禁忌

### 推荐做法
- 使用 `the primary font` 作为所有文本元素的主字体
- 使用 `#ffffff` 作为页面背景色
- 使用 `#000000` 作为主正文文本颜色
- 使用 `#efefef` 作为主强调色/交互色
- 保持大多数元素的圆角一致为 `10rpx`

### 避免做法
- 不要使用纯黑色（`#000000`）作为文本颜色——设计使用了更柔和的深色
- 不要使用过大的圆角（药丸形状）——设计偏好保守的圆角
- 不要添加厚重的阴影——设计刻意保持扁平
- 不要混用字体族——遵循已定义的字体系统
- 不要使用调色板之外的颜色
- 不要用随意的值覆盖间距系统


## 8. 响应式行为

### 断点
在可访问的样式表中未检测到明确的媒体查询断点。网站可能使用了：
- 容器查询
- 基于 JavaScript 的响应式逻辑
- CSS-in-JS 动态断点

### 触控目标
- 按钮在移动端应保持最小 44px 的触控目标
- 导航链接应有足够的间距以便触控交互
- 表单输入框在移动端应至少 44px 高

### 折叠策略
- 导航：水平链接 → 移动端汉堡菜单
- 多列布局 → 移动端单列堆叠
- 排版比例在较小视口上压缩
- 间距在移动端按比例缩小
- Flexbox 布局在较小屏幕上自然换行


## 9. AI 代理提示指南

### 快速颜色参考
- 背景: `#ffffff`
- 主文本: `#000000`
- 强调色 `#efefef`：用于 background, border（3次）
- 强调色 `#05b512`：用于 border（3次）
- 强调色 `#999999`：用于 border（1次）
- 强调色 `#494949`：用于 （1次）
- 强调色 `#42bd56`：用于 background（1次）

### 示例组件提示

### 迭代指南
1. 始终使用 `system font` 作为主字体
2. 字重: 
3. 页面背景：`#ffffff`，文本：`#000000`
4. 默认圆角: `10rpx`
6. 间距基础单位: 8px
